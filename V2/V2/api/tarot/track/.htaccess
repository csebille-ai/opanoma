Options -MultiViews
DirectoryIndex index.php

RewriteEngine On
# Laisse passer les fichiers/dossiers
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tout le reste → index.php
RewriteRule ^ index.php [L,QSA]
